# aws-static-site-s3-cloudfront
Deployed a static site to a private S3 bucket served through CloudFront, using Origin Access Control and a scoped bucket policy so objects are reachable only via the distribution and direct S3 access returns access denied.
